# This is the default user-endpoint-process (UEP) template provided with
# newly-configured endpoints.  Endpoints generate a UEP-specific configuration
# by processing this YAML file as a Jinja template against SDK-provided (user)
# variables -- please modify this template to suit your site's requirements.
#
# As an optional security and user-debugging aid, consider also specifying a
# JSON schema for the user-provided variables.  If `user_config_schema.json`
# exists within the same directory, then before starting the UEP, the MEP will
# validate the variables against the schema before rendering.  This provides
# an administrative peace of mind that users cannot specify invalid arguments.
# From a usability standpoint, however, it also can make invalid values
# prominently visible to users.
#
# For more information, please see the `user_endpoint_config` in Globus Compute
# SDK's Executor.
#
# Some common options site-administrators may want to set:
#  - address
#  - provider (e.g., SlurmProvider, TorqueProvider, CobaltProvider, etc.)
#  - account
#  - scheduler_options
#  - walltime
#  - worker_init
#
# There are a number of example configurations available in the documentation:
#    https://globus-compute.readthedocs.io/en/stable/endpoints.html#example-configurations

{%- set _rt = _GC.user_runtime %}
{%- set py_ver =
  (_rt.python.version_tuple | join("."))
  if _rt is defined and _rt.python is defined and _rt.python.version_tuple is defined
  else none
%}
{%- set sdk_ver =
  (_rt.globus_compute_sdk_version[1:-1])
  if _rt is defined and _rt.globus_compute_sdk_version is defined
  else none
%}

endpoint_setup: {{ endpoint_setup|default() }}

engine:
  type: GlobusComputeEngine
  max_workers_per_node: 1

  provider:
    type: LocalProvider

    min_blocks: 0
    max_blocks: 1
    init_blocks: 1

    # The default worker initialization creates (or reuses) a per-task Python
    # virtual environment with `uv`, matched to the submitting user's Python
    # and Globus Compute SDK versions.  If the user supplies a `requirements`
    # option -- a requirements.txt file as a multi-line string -- those
    # packages are installed into the environment at task-submission time.
    worker_init: |
      export PATH="$HOME/.local/bin:$PATH"
      if ! command -v uv &> /dev/null; then
        echo "uv command not found; installing from https://astral.sh/uv ..."
        curl -LsSf https://astral.sh/uv/install.sh | sh
      fi

      # Enable some useful performance tuning and debugging options.  See
      # https://docs.astral.sh/uv/reference/environment/ for details.
      export UV_LINK_MODE=hardlink
      export UV_COMPILE_BYTECODE=1
      export UV_TORCH_BACKEND=auto

      # Dependency Cooldown (recommended):
      # Uncomment to prevent `uv` from installing packages published within a
      # certain timeframe, giving maintainers time to fix a compromised release.
      # See https://docs.astral.sh/uv/reference/cli/#uv-run--exclude-newer
      # export UV_EXCLUDE_NEWER="1 week"

      # Build the environment name from the user's runtime.
{%- if py_ver or sdk_ver or (requirements is defined) %}
      ENV_NAME=""
{%- else %}
      ENV_NAME="default"
{%- endif %}
{%- if py_ver %}
      ENV_NAME="$ENV_NAME-py{{ py_ver }}"
{%- endif %}
{%- if sdk_ver %}
      ENV_NAME="$ENV_NAME-sdk{{ sdk_ver }}"
{%- endif %}
{%- if requirements is defined %}
      ENV_NAME="$ENV_NAME-$(echo -e {{ requirements }} | md5sum | awk '{print $1}')"
{%- endif %}
      ENV_SITE="$HOME/.globus_compute/venvs"
      ENV_PATH="$ENV_SITE/$ENV_NAME"

      # Do the deed & activate the environment, creating it first if it's missing.
      uv venv --allow-existing "$ENV_PATH"{% if py_ver %} --python {{ py_ver }}{% endif %}
      source "$ENV_PATH/bin/activate"

      # Install globus-compute-endpoint & user requirements, ignoring
      # dependency cooldowns for the former.
{%- if sdk_ver %}
      UV_EXCLUDE_NEWER=false uv pip install globus-compute-endpoint=={{ sdk_ver }}
{%- else %}
      UV_EXCLUDE_NEWER=false uv pip install globus-compute-endpoint
{%- endif %}
{%- if requirements is defined %}
      uv pip install -r <(echo -e {{ requirements }})
{%- endif %}

{%- if worker_init is defined %}
      # Run user-specified worker_init.
      eval '$(echo -e {{ worker_init }})'
{%- endif %}

# Endpoints will be restarted when a user submits new tasks to the
# web-services, so eagerly shut down if endpoint is idle.  At 30s/hb (default
# value), 10 heartbeats is 300s.
idle_heartbeats_soft: 10

# If endpoint is *apparently* idle (e.g., outstanding tasks, but no movement)
# for this many heartbeats, then shutdown anyway.  At 30s/hb (default value),
# 5,760 heartbeats == "48 hours".  (Note that this value will be ignored if
# idle_heartbeats_soft is 0 or not set.)
idle_heartbeats_hard: 5760
